use repdb::get_connection;
// use repdb::contact::get_contact;

fn main() {
    let _conn = get_connection();
    // let c = get_contact(&conn, 2).unwrap();
    // // println!("{}", c_lists.len());
    // // for item in c_lists {
    // //     println!("{} {:?} {:?}", item.id, item.num, item.cert_date);
    // // }
    // // println!("{:?}", c);
    // println!("{:?}", c.id);
    // println!("{:?}", c.emails);
    // println!("{:?}", c.phones);
    // println!("{:?}", c.educations);
}
